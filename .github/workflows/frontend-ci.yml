name: Frontend CI

on:
  push:
    branches: [main, develop]
    paths:
      - 'web/**'
  pull_request:
    branches: [main, develop]
    paths:
      - 'web/**'

jobs:
  type-check:
    name: TypeScript Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: web

      - name: TypeScript type check
        run: npx tsc --noEmit
        working-directory: web

  # TODO: Uncomment when web app is production-ready
  # lint-and-build:
  #   name: Lint & Build
  #   steps:
  #     - run: npm run lint
  #     - run: npm run build
  #
  # test:
  #   name: Unit Tests
  #   steps:
  #     - run: npm test -- --run
